#include <stdio.h>

int main()
{
    int i, j;
    char a[3], b[3];
    scanf("%s %s", a, b);

    for(i=0;i<2;i++)
    {
        for(j=0;j<2;j++)
        {
            if(a[i]=='A' && b[j]=='A')
        }
    }
    return 0;
}
